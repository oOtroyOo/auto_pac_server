var PROXY = "SOCKS5 $serverIp$:$serverPort$; SOCKS $serverIp$:$serverPort$; DIRECT;";

var domains = [
    $domains$,
    'community.akamai.steamstatic.com',
    'githubusercontent.com',
    'stackoverflow.com',
    'cloudfront.net',
    'akamaihd.net'
];

function FindProxyForURL(url, host) {
    for (var i = domains.length - 1; i >= 0; i--) {
        if (dnsDomainIs(host, domains[i])) {
            return PROXY;
        }
    }
    return "DIRECT";
}